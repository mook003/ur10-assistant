from setuptools import setup, find_packages

setup(
    name='URBasic',
    version='0.1.0',
    packages=find_packages(),
    description='URBasic library for Universal Robots',
    author='Original Author',
    install_requires=[],
    python_requires='>=3.6',
)
